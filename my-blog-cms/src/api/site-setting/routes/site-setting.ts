/**
 * site-setting router
 */

import { factories } from '@strapi/strapi';

// @ts-ignore - Type will be generated after first run
export default factories.createCoreRouter('api::site-setting.site-setting' as any);
