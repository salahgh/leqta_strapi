/**
 * site-setting service
 */

import { factories } from '@strapi/strapi';

// @ts-ignore - Type will be generated after first run
export default factories.createCoreService('api::site-setting.site-setting' as any);
